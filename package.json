{
  "name": "workspace",
  "version": "0.1.0",
  "description": "Read-only engineering workspace search and file access",
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24.12.0"
  },
  "dependencies": {
    "@actiondock/sdk": "^2.4.0",
    "@vscode/ripgrep": "^1.18.0"
  },
  "devDependencies": {
    "@actiondock/runtime-node": "^2.4.0",
    "@actiondock/testing": "^2.4.0",
    "@types/node": "^22.13.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
