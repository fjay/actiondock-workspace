# Agent 开发协作指引 - knowledge-server

- **核心定位**：knowledge-server 是基于 ActionDock 规范构建的云主机一体化知识服务容器分发，支持双 HTTP 服务架构：面向外部查询用户通过动作级白名单提供只读检索与反馈追加服务，面向维护智能体提供具备完整读写与维护能力的受控服务。
- **运行时与技术栈**：Node.js（版本大于等于 24.12.0）、Docker、Docker Compose、Git 与 ripgrep。
- **代码库分层结构**：
  - `packages/knowledge-workspace`：工作区能力包，提供基于 ripgrep 的工程代码与文档全文检索、受控文件分段直读、目录浏览、安全写入、受控编辑、删除移动、状态差异审查与断链校验。
  - `packages/knowledge-inbox`：反馈追加平面能力包，负责收集人工排障与日常运维产生的结构化候选文档并写入待审池。
  - `packages/knowledge-maintenance`：特权维护平面能力包，包含双分支代码同步、差异扫描、文档发布提交与检查点推进动作及后台调度驱动脚本。
  - `host/knowledge-maintenance`：云主机宿主机封装脚本，供维护智能体连入宿主机时便捷调用容器内工具。
  - `config/repos.json.example`：多代码仓与系统知识仓清单配置模板。
  - `skills/project-knowledge-maintainer`：配套知识维护智能体技能规范与参考模板。
  - `skills/knowledge-maintenance-orchestrator`：知识维护总控编排技能，负责分支同步、差异扫描、子智能体调度与全流程闭环推进。
  - `Dockerfile` 与 `docker-compose.yml`：一体化容器构建与编排配置。
  - `entrypoint.sh`：容器启动自举脚本，负责全局环境配置、包路由自动软链与双 HTTP 服务监听。
- **常用验证与构建命令**：
  - 执行子包单元测试：进入对应子包目录执行 `npm test`。
  - 执行类型检查：进入对应子包目录执行 `npm run typecheck`。
  - 本地容器镜像构建验证：`docker compose build`。
  - **文档改动免测准则**：若改动仅涉及文档、说明书或注释，严禁执行全量测试与镜像构建命令，直接完成文档编辑即可。
- **核心工程设计原则**：
  - **双服务分流与动作级白名单**：查询服务与维护服务物理分流。查询服务面向外部用户，基于动作级白名单严格收敛，仅暴露只读检索与候选收集动作，严禁提供任何写操作；维护服务面向受信任网络与维护智能体，提供完整工作区读写、编辑、审查与维护闭环能力。
  - **解耦优先**：核心业务与底层环境解耦，候选贡献单元与正式知识单元解耦，杜绝机械映射。
  - **单一职责**：各工具包职责边界清晰，工作区平面操作严格收敛在根目录内，反馈平面仅允许追加受控候选文档。
  - **单一事实源**：Git 仓库作为正式知识的唯一事实源，云端知识中枢对外提供最新只读视界。
  - **非阻塞异步**：代码执行链路保持非阻塞异步，确保服务高吞吐与灵敏响应。
  - **防御与透明**：严禁静默吞没异常，坚持路径穿越校验、凭据过滤与全链路错误透传。
  - **检查点推进闭环**：代码变更驱动知识有效性检查，无论文档是否需要更新，维护流程结束后均须推进检查点水位。
- **文档与输出风格规范**：
  - **禁止使用数字序号**：排版一律采用无序列表符号（`- `），不得使用数字列表。
  - **严禁使用表情符号**：严禁在文档或输出中添加任何表情符号与图标。
  - **杜绝中英文夹杂**：标题、括号及正文中不得添加非必要的英文翻译和短语，仅保留必要的类名、方法名、包名、命令与专有名词。
  - **严禁粗体嵌套行内代码**：严禁在 Markdown 粗体标签内部嵌套行内代码反引号，行内代码必须独立于粗体之外书写。
  - **规范术语表述**：严禁使用生硬词汇，统一表述为「框架」、「产物」或「资产」等规范清晰术语。
- **代码提交与协作规范**：
  - **规范提交格式**：提交信息统一遵循约定式提交规范，如 `feat:`、`fix:`、`docs:`、`chore:` 等前缀。
  - **保持工作区整洁**：提交前确保无临时日志、本地密钥或非必要产物落盘，保护环境安全。
