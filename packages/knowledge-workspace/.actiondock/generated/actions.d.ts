/**
 * AUTO-GENERATED BY ACTIONDOCK. DO NOT EDIT DIRECTLY.
 * @manifest-digest sha256-add56b8e9d3261a1442c65d4f14a9b5a6b3994c68401fdc40f4ef689e3616a24
 */

import type { ActionContext, ActionDefinition } from "@actiondock/sdk";

export namespace Actions {
  export namespace SearchRg {
    /**
     * Search text in the engineering workspace using ripgrep semantics. Most input fields map directly to ripgrep long options. Paths are relative to the workspace root.
     */
    export type Input = {
      /** ripgrep search pattern (regular expression by default, or literal if fixedStrings is true) */
      pattern: string;
      /** Paths to search, relative to workspace root. Defaults to workspace root if omitted. */
      paths?: Array<string>;
      /** Treat pattern as literal string instead of regular expression (-F) */
      fixedStrings?: boolean;
      /** Case insensitive search (-i) */
      ignoreCase?: boolean;
      /** Smart case search: case-insensitive unless pattern contains uppercase (-S) */
      smartCase?: boolean;
      /** Only match whole words (-w) */
      wordRegexp?: boolean;
      /** Only match whole lines (-x) */
      lineRegexp?: boolean;
      /** Glob patterns to include or exclude (e.g. ['*.ts', '!generated/**']) (-g) */
      glob?: Array<string>;
      /** Only search files matching file type definition (e.g. ['ts', 'py']) (--type) */
      type?: Array<string>;
      /** Do not search files matching file type definition (--type-not) */
      typeNot?: Array<string>;
      /** Search hidden files and directories (--hidden) */
      hidden?: boolean;
      /** Do not respect .gitignore or other ignore files (--no-ignore) */
      noIgnore?: boolean;
      /** Follow symbolic links (--follow) */
      follow?: boolean;
      /** Show NUM lines before and after each match (-C) */
      context?: number;
      /** Show NUM lines before each match (-B) */
      beforeContext?: number;
      /** Show NUM lines after each match (-A) */
      afterContext?: number;
      /** Enable matching across multiple lines (-U) */
      multiline?: boolean;
      /** Make '.' in regex match newlines (--multiline-dotall) */
      multilineDotall?: boolean;
      /** Maximum total number of matches to return across all files. Defaults to 200 (hard cap 200). */
      maxResults?: number;
      /** Limit number of matching lines per file (-m). Note: this limits matches per file, not global total matches. */
      maxCount?: number;
      /** Don't print lines longer than this limit in bytes (--max-columns) */
      maxColumns?: number;
      /** Print preview for lines exceeding maxColumns (--max-columns-preview) */
      maxColumnsPreview?: boolean;
    };
    export type Output = {
      matches: Array<{
        path: string;
        line: number;
        text: string;
        before?: Array<{
          line: number;
          text: string;
        }>;
        after?: Array<{
          line: number;
          text: string;
        }>;
        truncated?: boolean;
      }>;
      truncated: boolean;
    };
  }

  export namespace FilesRead {
    /**
     * Read UTF-8 text from a file in the engineering workspace. Supports reading from a specific line with bounded output. Paths are relative to the workspace root.
     */
    export type Input = {
      /** Path to the file relative to the workspace root */
      path: string;
      /** 1-based starting line number. Defaults to 1. */
      startLine?: number;
      /** Maximum number of lines to read. Defaults to 2000. */
      maxLines?: number;
    };
    export type Output = {
      path: string;
      startLine: number;
      endLine: number;
      content: string;
      hasMore: boolean;
      truncated?: boolean;
    };
  }

  export namespace FilesList {
    /**
     * List files and directories in the engineering workspace. Paths are relative to the workspace root.
     */
    export type Input = {
      /** Directory path relative to workspace root. Defaults to workspace root. */
      path?: string;
      /** Maximum directory traversal depth. Defaults to 1. */
      depth?: number;
      /** Whether to include hidden files. Defaults to false. */
      hidden?: boolean;
    };
    export type Output = {
      items: Array<{
        path: string;
        type: "file" | "directory";
        sizeBytes?: number;
      }>;
      truncated: boolean;
    };
  }

  export namespace FilesWrite {
    /**
     * Write UTF-8 text content to a file in the workspace. Supports automatic parent directory creation and overwrite control.
     */
    export type Input = {
      /** Path to the file relative to the workspace root */
      path: string;
      /** Plain text content to write to the file */
      content: string;
      /** Whether to automatically create parent directories if they do not exist. Defaults to true. */
      createDirs?: boolean;
      /** Whether to overwrite an existing file. Defaults to true. */
      overwrite?: boolean;
    };
    export type Output = {
      /** Normalized path relative to the workspace root */
      path: string;
      /** Number of bytes written to the file */
      bytesWritten: number;
      /** Whether a new file was created */
      created: boolean;
    };
  }

  export namespace FilesEdit {
    /**
     * Edit a file in the workspace by replacing target text content. Supports line range constraints and multiple replacements control.
     */
    export type Input = {
      /** Path to the file relative to the workspace root */
      path: string;
      /** Exact text content to find and replace */
      targetContent: string;
      /** Replacement text content */
      replacementContent: string;
      /** Whether to allow replacing multiple occurrences. Defaults to false. */
      allowMultiple?: boolean;
      /** 1-based starting line number to constrain the search range */
      startLine?: number;
      /** 1-based ending line number to constrain the search range */
      endLine?: number;
    };
    export type Output = {
      /** Path relative to the workspace root */
      path: string;
      /** Number of occurrences replaced */
      replacementsCount: number;
      /** Total byte size of the file after replacement */
      bytesWritten: number;
    };
  }

  export namespace BashExec {
    /**
     * Execute a bash shell command in the engineering workspace.
     */
    export type Input = {
      /** Bash command line string to execute */
      command: string;
      /** Working directory path relative to workspace root. Defaults to workspace root. */
      cwd?: string;
      /** Execution timeout in milliseconds. Defaults to 60000 (60 seconds). */
      timeoutMs?: number;
    };
    export type Output = {
      /** Process exit code (0 indicates success) */
      exitCode: unknown;
      /** Standard output text of the command */
      stdout: string;
      /** Standard error text of the command */
      stderr: string;
    };
  }

  export namespace LinksVerify {
    /**
     * Verify local relative links and heading anchor references in markdown documentation.
     */
    export type Input = {
      /** Path to a directory or markdown file to scan relative to workspace root. Defaults to '.'. */
      path?: string;
      /** Whether to verify document heading anchor targets. Defaults to true. */
      checkAnchors?: boolean;
      /** Additional directory names to ignore during scanning. */
      ignoreDirs?: Array<string>;
    };
    export type Output = {
      /** Number of markdown files scanned */
      scannedFiles: number;
      /** Total number of local relative links checked */
      totalLinks: number;
      /** Total number of broken links found */
      brokenCount: number;
      /** List of broken links found during scan */
      brokenLinks: Array<{
        /** Relative path to markdown file containing the broken link */
        file: string;
        /** 1-based line number where the broken link appears */
        line: number;
        /** Raw link text as written in document */
        link: string;
        /** Resolved relative target path or anchor */
        target: string;
        /** Failure reason code (TARGET_NOT_FOUND or ANCHOR_NOT_FOUND) */
        reason: "TARGET_NOT_FOUND" | "ANCHOR_NOT_FOUND";
      }>;
    };
  }
}

export interface ActionSchemaMap {
    "search.rg": {
      Input: Actions.SearchRg.Input;
      Output: Actions.SearchRg.Output;
    };
    "files.read": {
      Input: Actions.FilesRead.Input;
      Output: Actions.FilesRead.Output;
    };
    "files.list": {
      Input: Actions.FilesList.Input;
      Output: Actions.FilesList.Output;
    };
    "files.write": {
      Input: Actions.FilesWrite.Input;
      Output: Actions.FilesWrite.Output;
    };
    "files.edit": {
      Input: Actions.FilesEdit.Input;
      Output: Actions.FilesEdit.Output;
    };
    "bash.exec": {
      Input: Actions.BashExec.Input;
      Output: Actions.BashExec.Output;
    };
    "links.verify": {
      Input: Actions.LinksVerify.Input;
      Output: Actions.LinksVerify.Output;
    };
}

export type ActionInput<T extends keyof ActionSchemaMap> = ActionSchemaMap[T]["Input"];
export type ActionOutput<T extends keyof ActionSchemaMap> = ActionSchemaMap[T]["Output"];

export type TypedActionDefinition<T extends keyof ActionSchemaMap> = ActionDefinition<
  ActionSchemaMap[T]["Input"],
  ActionSchemaMap[T]["Output"]
>;
